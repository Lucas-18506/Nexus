# 部署说明 - IWM Investment Advisor

## 部署架构

```
┌─────────────────────────────────────────┐
│  Render 云服务器 (Docker)                │
│                                         │
│  ┌──────────────┐  ┌──────────────────┐ │
│  │  FastAPI     │  │  React 静态文件  │ │
│  │  后端 API    │  │  (build产物)     │ │
│  │  Port 8000   │  │  由后端 serve    │ │
│  └──────┬───────┘  └──────────────────┘ │
│         │                               │
│         └──────────────┐                │
│                        ▼                │
│  ┌──────────────────────────────────┐   │
│  │  Render PostgreSQL (免费 1GB)   │   │
│  │  DATABASE_URL 环境变量自动注入    │   │
│  └──────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

## 部署配置清单

### 已创建的文件

| 文件 | 说明 |
|---|---|
| `Dockerfile` | 多阶段构建：前端 build + 后端打包 |
| `render.yaml` | Render Blueprint 配置（含 PostgreSQL） |
| `Procfile` | 启动命令（备用） |
| `start.sh` | 启动脚本（初始化 DB + 启动 uvicorn） |
| `app/core/config.py` | 支持 `DATABASE_URL` 环境变量 |
| `app/main.py` | 添加 CORS + 静态文件服务 + 数据库初始化 |

### 部署步骤

#### 1. 创建 GitHub 仓库（如果还没有）

```bash
cd /path/to/Nexus
# 创建 .gitignore
echo "node_modules/\n__pycache__/\n.env\n*.db\n" > .gitignore
git init
git add .
git commit -m "Initial deployment setup"
# 在 GitHub 创建仓库后
git remote add origin https://github.com/YOUR_USERNAME/iwm-deploy.git
git push -u origin main
```

#### 2. Render 部署

1. 访问 [render.com](https://render.com) 注册/登录
2. 点击 **New +** → **Blueprint**
3. 连接你的 GitHub 仓库
4. Render 会自动读取 `render.yaml` 配置
5. 创建服务时设置环境变量：
   - `OPENAI_API_KEY`（你的 OpenAI API Key）
   - 其他可选：`QDRANT_API_KEY`, `QDRANT_HOST`

#### 3. 环境变量配置

| 变量 | 来源 | 说明 |
|---|---|---|
| `DATABASE_URL` | Render 自动注入 | PostgreSQL 连接串 |
| `OPENAI_API_KEY` | 手动配置 | 大V监测/Agent 分析必需 |
| `QDRANT_HOST` | 可选 | 向量库地址 |
| `QDRANT_API_KEY` | 可选 | 向量库密钥 |
| `ANALYSIS_REPORTS_DIR` | 默认 `/app/analysis_reports` | 分析报告目录 |
| `LOG_LEVEL` | 默认 `INFO` | 日志级别 |

#### 4. 构建部署

Render 会自动：
1. 执行 Dockerfile 多阶段构建
2. 构建前端（`npm run build` → `dist/`）
3. 安装 Python 依赖
4. 启动服务（`start.sh`）
5. 初始化数据库表（`init_db()`）

#### 5. 验证部署

```bash
# 健康检查
curl https://your-app-name.onrender.com/health

# 测试 API
curl https://your-app-name.onrender.com/api/portfolio/positions
```

## 已知限制

### Render 免费版限制
- **Web 服务**: 15 分钟无请求后休眠，首次请求需 30-60s 冷启动
- **PostgreSQL**: 1GB 存储，30 天后自动删除（需手动续期）
- **无持久化文件系统**: 上传的分析报告会在重启后丢失

### 解决方案
- 使用 [Render 付费版](https://render.com/pricing)（$7/月 起）避免休眠
- 或改用 Railway（更稳定）
- 分析报告建议保存到云存储（AWS S3 / Cloudflare R2）

### Qdrant 向量库
- Render 免费版不支持多服务部署
- 建议：使用 Qdrant Cloud 免费版（1GB 向量存储）
- 或临时禁用向量搜索功能

## 需要用户做的事

1. [ ] **注册 Render 账号**: [render.com](https://render.com)
2. [ ] **准备 GitHub 仓库**: 推送代码到 GitHub
3. [ ] **配置 OPENAI_API_KEY**: 在 Render Dashboard 添加环境变量
4. [ ] **（可选）配置域名**: Render 自动生成 `.onrender.com` 域名，可绑定自定义域名

## 本地测试构建

```bash
cd /path/to/Nexus

# 构建 Docker 镜像
docker build -t iwm-app .

# 运行（需本地 PostgreSQL）
docker run -p 8000:8000 -e DATABASE_URL=postgresql://... iwm-app

# 访问 http://localhost:8000
```

## 备选方案：Railway

如果 Render 体验不佳，可以改用 Railway：

1. 访问 [railway.app](https://railway.app)
2. 从 GitHub 部署
3. 添加 PostgreSQL 服务
4. 配置环境变量

Railway 优势：
- 无冷启动问题
- 更简单的多服务编排
- 支持 SQLite 作为免费替代

---

*部署配置已就绪，等待用户执行 GitHub 推送和 Render 部署步骤。*
